// STRING TEMPLATES
const name = "Mikami";
const lastName = "Teru";

const withoutTemplate = name + " " + lastName;
const withTemplate = `${name} ${lastName}`;

console.log(withoutTemplate);
console.log(withTemplate);
