// TERNARY CONDITIONAL OPERATOR

const active = true;

const message = active ? "Active" : "Inactive";

// Only response when is true or false
const message2 = active && "Active";

console.log({ message, message2 });
